Your task: 
Refactor ONLY the UI layout and styling of this page to make it fully mobile-responsive. 
Do NOT modify any of the app’s logic, database functions, API calls, or component structure unless required for layout.

Requirements:
1. Apply mobile-first responsive design.
2. Ensure all content stacks cleanly on small screens (iPhone/Android).
3. Add or update CSS using:
   • fluid widths (100%)
   • max-width containers for desktop
   • consistent padding
   • readable font sizes on mobile
   • buttons full-width on mobile
4. Add the correct viewport meta tag if missing:
   <meta name="viewport" content="width=device-width, initial-scale=1">
5. Any two-column areas must automatically collapse into a single-column layout on screens under 768px.
6. Clean spacing, fix overflow issues, and ensure forms and cards do not go off-screen.
7. Use this mobile-first structure:
   - .page (wrapper)
   - .main-container (centered content)
   - .card (sections)
   - .layout-2col (becomes side-by-side on desktop, stacked on mobile)
8. Keep the dark UI color scheme exactly the same.

Add this CSS (modify only where needed to match my file structure):

body {
  margin: 0;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: #0b1020;
  color: #f5f5f5;
}

.main-container {
  width: 100%;
  max-width: 480px;
  margin: 0 auto;
  padding: 16px;
  box-sizing: border-box;
}

.card {
  background: #151a2c;
  border-radius: 14px;
  padding: 16px;
  margin-bottom: 16px;
}

form {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

input, select, textarea {
  width: 100%;
  padding: 10px 12px;
  border-radius: 10px;
  background: #0f1424;
  color: #f5f5f5;
  border: 1px solid #2b3450;
}

button {
  width: 100%;
  padding: 12px;
  border-radius: 999px;
  font-weight: 600;
  background: #2563eb;
  color: #fff;
  border: none;
}

.layout-2col {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

@media (min-width: 768px) {
  .main-container {
    max-width: 960px;
    padding: 24px 32px;
  }

  .layout-2col {
    flex-direction: row;
  }

  .layout-2col > .col {
    flex: 1;
  }

  button {
    width: auto;
  }
}

Your goal:
Make the UI look clean and smooth on phones without breaking anything. 
Return updated code with improved mobile responsiveness.
